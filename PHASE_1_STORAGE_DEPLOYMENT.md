# ThriveX 一期任务清单：存储与部署基础

## 目标

新部署默认使用本地存储，不配置第三方服务也能上传、浏览和管理图片；云存储统一通过 S3 接入 Oracle OCI 或腾讯 COS；七牛专属实现最终移除。

## 执行规则

- 每轮只执行一个编号子任务；未完成时明确说明原因，不跳过或虚报完成。
- 每轮汇报包含：本轮任务编号、完成内容、清单状态、下一任务编号。
- 本文件是唯一进度来源；每有实质变化都同步更新。
- 标记：`[ ]` 未开始、`[~]` 进行中、`[x]` 已完成、`[-]` 取消或需重做。

## 1. 规划与现状确认

- [x] 1.1 建立一期范围、目标和进度文档。
- [x] 1.2 只读审查已有改动，确认 Compose、本地存储和抽象层均未完成交付。
- [x] 1.3 固化最终存储架构与验收标准：本地/S3 职责、配置来源、公开 URL、错误行为。
- [x] 1.4 依据 1.3 决定此前未完成代码的保留、重构或删除范围。

## 2. 默认本地存储

- [x] 2.1 文件服务只依赖统一存储选择器，不直接依赖具体 provider。
- [x] 2.2 完成本地 provider：上传、列表、树、详情、删除和目录操作。
- [x] 2.3 统一相对路径、完整 URL 解析及反向代理下的公开 URL。
- [x] 2.4 补充本地文件操作与非法路径防护测试。

## 3. S3 兼容存储

- [x] 3.1 引入 S3 SDK，并定义仅服务端读取的配置模型。
- [x] 3.2 实现 S3 provider：上传、列表、树、详情、删除和逻辑目录操作。
- [x] 3.3 实现选择器：默认 local，可选 s3；无效配置返回可诊断错误。
- [x] 3.4 实现连接测试：验证 endpoint、凭据、bucket 的读写/删除权限。
- [x] 3.5 编写 OCI 与腾讯 COS 的配置及验收说明。

## 4. 删除七牛专属实现

- [x] 4.1 删除七牛 SDK、配置类、异常及压缩任务依赖。
- [x] 4.2 删除后端七牛图片瘦身接口和后台操作。
- [x] 4.3 删除后台七牛配置页、类型和初始化入口。
- [x] 4.4 更新新部署 SQL 种子配置，移除七牛项。

## 5. Compose 部署交付

- [x] 5.1 重整 Compose：Blog、Admin、Server、MySQL、反向代理。
- [x] 5.2 配置数据卷与健康检查。
- [x] 5.3 补齐环境变量、首次启动、升级、备份与恢复说明。

## 6. 验证与文档

- [x] 6.1 后端编译和本地/S3 provider 测试。
- [x] 6.2 Admin、Blog lint/build。
- [x] 6.3 Compose 校验和首次部署验收。
- [x] 6.4 更新最终验证记录与已知限制。

## 暂不纳入一期

- 数据库版本迁移、自动升级、后台数据库备份/导入/恢复。
- 初始化向导重构、安全改造及邮件、地图、AI 等功能。

## 1.3 设计结论

### 存储职责

| 模式 | 用途 | 默认值 | 数据位置 |
| --- | --- | --- | --- |
| `local` | 新部署、个人服务器、无云服务场景 | 是 | 后端 `file.dir` 目录；Compose 挂载命名卷 |
| `s3` | Oracle OCI、腾讯 COS 或其他 S3 兼容服务 | 否 | 指定 bucket 与可选 prefix |

文件服务只能依赖 `StorageProvider` 和存储选择器；不允许在业务服务、控制器或前端中出现云厂商 SDK 类型。七牛不属于最终支持模式。

### 配置来源

| 配置 | 来源 | 规则 |
| --- | --- | --- |
| `STORAGE_PROVIDER` | 后端环境变量 | 仅允许 `local` 或 `s3`；缺省 `local` |
| 本地目录与公开地址 | 后端环境变量 | `FILE_DIR`、`FILE_PUBLIC_URL`；不从浏览器推断外部域名 |
| S3 endpoint、region、bucket、prefix、path-style | 后端环境变量 | 仅在 `s3` 模式要求填写 |
| S3 access key、secret key | Docker secrets 或后端环境变量 | 永不写入数据库、前端构建产物或接口响应 |

后台一期只显示当前模式、健康状态与非敏感配置摘要；凭据写入由服务端处理，绝不回传明文。

### 路径与公开 URL

- provider 内部只处理标准化的相对 key，例如 `article/abc.webp`；拒绝 `..`、绝对路径和任意外站 URL。
- 本地公开 URL 固定为 `${FILE_PUBLIC_URL}/static/upload/<key>`；未配置时返回相对 `/static/upload/<key>`。
- S3 公开 URL 优先使用可选 `S3_PUBLIC_URL`，否则由 endpoint/bucket/key 生成；一期只支持公开读取对象，不引入签名访问。
- 删除、详情、目录操作均只接收相对 key，不接受完整 URL。

### 错误与连接测试

- 无效 provider：返回“存储模式无效”。
- S3 缺字段：返回缺失字段名；鉴权、bucket、网络失败分别返回可操作的中文提示，详细堆栈仅记日志。
- 本地目录不存在时自动创建；不可写时返回“本地上传目录不可写”。
- 连接测试由后端执行：创建随机测试对象、读取元信息、列举、删除；最终只返回成功或分类失败原因。

### 验收标准

1. 不设置任何云存储变量时，本地模式可完成上传、列表、详情、删除和目录操作。
2. 本地文件在反向代理部署下返回稳定的公开 URL；重启容器后文件仍在挂载卷中。
3. Oracle OCI 与腾讯 COS 均可通过同一 S3 provider 完成连接测试和上述文件操作。
4. 前端不包含 S3 凭据；日志不输出密钥。
5. 删除七牛代码、依赖、配置与压缩入口后，后端、Admin、Blog 构建通过。

## 1.4 现有半完成改动处置

| 现有项 | 决定 | 原因与后续任务 |
| --- | --- | --- |
| `StorageProvider` 接口 | 保留并在 2.1 微调 | 契约方向正确，但文件服务尚未使用它。 |
| `LocalStorageConfig` | 重构，不直接验收 | 基础文件操作可复用；完整 URL 解析和从请求推导公开 URL 违反 1.3 设计。 |
| `FileServiceImpl` 的 local/qiniu 条件分支 | 删除并在 2.1 重写 | 服务仍直接依赖具体 provider，无法支持 S3。 |
| `storage.provider` 当前配置 | 保留占位，3.1 扩展 | `local` 默认值正确；需补 S3 变量和严格校验。 |
| 根目录 `compose.yaml` 与 `.env.example` | 保留，5.1–5.3 重整 | 数据卷思路正确，但尚非完整部署。 |
| `QiniuStorageConfig`、pfop 压缩任务与接口 | 在 4.1–4.2 删除 | 与一期最终架构不一致。 |
| Admin 七牛表单、初始化存储步骤和类型 | 在 4.3 删除 | 凭据不再保存于数据库或前端。 |
| SQL `qiniu_storage` 种子项 | 在 4.4 删除 | 新部署不应再出现七牛配置。 |

结论：不把当前半完成代码标记为任何 2.x–6.x 成果。先在 2.1 以 `StorageProvider` 为唯一依赖重构文件服务，再逐项完成后续任务。

## 当前状态说明

此前已有 Compose、本地 provider 与 `StorageProvider` 的未完成代码，尚不算 2.x–6.x 的已完成成果。任务 1.4 将按最终设计决定其保留、重构或删除。

## 验证记录

- 2026-09-19：任务 1.1、1.2 已完成；本轮仅重整任务清单，未改业务代码。
- 2026-09-19：任务 1.3 已完成；已记录最终存储设计与验收标准，未改业务代码。
- 2026-09-19：任务 1.4 已完成；已完成只读处置审查，未改业务代码。
- 2026-09-19：任务 2.1、2.3 编译通过；本地 provider 使用 `FILE_PUBLIC_URL` 生成公开地址，并兼容本站完整 URL 与相对 key。
- 2026-09-19：任务 2.4 已完成；`LocalStorageConfigTest` 覆盖文件与目录操作、完整 URL 解析和路径越界防护，Maven 测试通过。
- 2026-09-19：任务 3.1 已完成；引入 AWS S3 SDK，S3 配置只读取后端环境变量，Maven 编译通过。
- 2026-09-19：已校正任务 1.4 的清单状态；任务 3.2 开始实施。
- 2026-09-19：任务 3.2 已完成；新增 S3 provider，支持对象上传、列表、树、详情、删除及逻辑目录操作，Maven 测试通过。
- 2026-09-19：任务 3.3 已完成；选择器默认 local，仅接受 local/s3，选择 s3 时校验必填环境变量；Maven 测试通过。
- 2026-09-19：任务 3.4 已完成；新增当前存储连接测试接口，S3 通过临时对象验证 bucket 的写入、读取、列举与删除权限，并对配置、鉴权、bucket、网络错误返回分类提示；Maven 测试通过。
- 2026-09-19：任务 3.5 已完成；更新对象存储文档，提供 OCI 与腾讯 COS 的 S3 环境变量示例、寻址方式、权限要求与验收步骤，移除控制端七牛密钥配置指引。
- 2026-09-19：任务 4.1 已完成；已移除七牛 SDK、配置、异常处理、pfop 压缩任务、模型与后端压缩接口，并消除文件服务对七牛异常的依赖；Maven 测试通过。经 Admin 复查，文件管理页仍有调用已删除压缩接口的操作，任务 4.2 需继续清理该残留入口。
- 2026-09-19：任务 4.3 已完成；移除 Admin 七牛配置表单、第三方配置入口、配置类型与初始化存储步骤；Admin lint 因当前 WSL Node.js 运行环境不可用而未执行。
- 2026-09-19：任务 4.4 已完成；新部署 SQL 的 env_config 种子已移除 qiniu_storage 项。
- 2026-09-19：任务 4.2 已完成；移除 Admin 文件管理页的图片瘦身按钮、确认/进度弹层、轮询逻辑、接口声明、类型及专属动画，消除对已删除后端接口的残留调用。
- 2026-09-19：任务 5.1 已完成；Compose 统一编排 MySQL、Server、Blog、Admin 与 Nginx 反向代理；外部仅暴露代理端口，浏览器 API 固定走 /api，Blog 服务端渲染走 Docker 内部 Server 地址；Compose 配置校验通过。
- 2026-09-19：任务 5.2 已完成；MySQL 数据与本地上传目录分别使用命名卷持久化；为 MySQL、Server、Blog、Admin 与 Nginx 配置健康检查，并以健康状态约束容器启动依赖；Compose 配置校验通过。
- 2026-09-19：任务 5.3 已完成；扩充 .env.example，新增 Compose 部署文档和导航入口，覆盖变量、首次启动、升级、MySQL/本地上传备份恢复及 S3 边界；使用该样例的 Compose 配置校验通过。
- 2026-09-19：任务 6.1 已完成；执行 `mvn -pl blog -am test`，后端编译通过，LocalStorageConfig、S3StorageProvider 与 StorageProviderSelector 共 8 项测试全部通过。
- 2026-09-19：任务 6.2 已完成；使用 Node 20.20.2 完成 Admin lint 与生产构建；Blog ESLint 为 0 error、1 条既有 warning，并通过其 Dockerfile 完成生产镜像构建。首次本机 Blog Next 构建会话悬挂，改由与部署一致的 Docker 构建复核通过。
- 2026-09-19：任务 6.3 已完成；以 .env.example 完成 Compose 首次构建和启动，五个服务均健康；修复 S3 provider 多构造函数的 Spring 注入标注及 Nginx IPv4 健康探针，Blog、Admin、/api/user/author 入口均返回 HTTP 200；验收容器已停止，MySQL 与上传命名卷保留。
- 2026-09-19：任务 6.4 已完成；已汇总一期验证结果和已知限制。后端 8 项测试、Admin lint/build、Blog lint/Docker build、Compose 语法与首次启动验收均通过。

## 一期最终验证结论

- 默认 `local` 模式已通过后端测试和 Compose 首启验证；上传目录由 `upload-data` 命名卷持久化。
- S3 provider 的配置校验、对象操作与分类错误已由单元测试覆盖；OCI 与腾讯 COS 的环境变量和人工验收步骤已写入对象存储文档。
- Admin 与 Blog 的生产构建均通过；Compose 五服务在 `.env.example` 配置下均达到健康状态，Blog、Admin 与公开 API 入口均返回 HTTP 200。
- 七牛依赖、后端压缩接口、Admin 配置及 SQL 配置种子均已移除。

## 已知限制

- 未使用真实 OCI 或腾讯 COS 凭据执行联网对象操作；部署时必须按对象存储文档完成连接测试、上传和匿名公开读取验收。
- Compose 默认只提供 HTTP；生产 HTTPS 需由上游网关、CDN 或补充 TLS Nginx 配置终止。
- Blog ESLint 仍有 1 条既有的无效 `eslint-disable` warning，但没有 lint error；未在本任务中扩大范围处理该历史警告。
- MySQL 初始化 SQL 只会在空数据卷首次创建时导入；一期不提供自动数据库迁移或自动升级，升级前须按 Compose 文档备份。
